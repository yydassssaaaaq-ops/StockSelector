# reports

当前包含真实行情筛选报告输出：

- `screen_report.py`：生成原始行情 CSV、候选 CSV、摘要 JSON 和 HTML 报告。
- `backtest_report.py`：生成最小历史回测 HTML、summary JSON、periods CSV、holdings CSV、failures CSV 和 universe CSV。
- 最新报告默认写入 `outputs/a_share_screen/latest.html`。
- 最新回测报告默认写入 `outputs/minimal_backtest/latest.html`。

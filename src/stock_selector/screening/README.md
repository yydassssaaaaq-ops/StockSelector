# screening

当前包含第一条规则选股链：

- `momentum_liquidity.py`：对真实行情快照进行过滤、打分和排序。
- 当前规则偏工程验证：动量、流动性、估值和波动守门项，不生成交易指令。
- 评分采用动态可用因子权重：缺失主力资金、估值或换手率时不按 0 分处理，而是记录缺失因子、实际有效权重和数据完整性。
- 快照筛选默认仍要求换手率；历史回测在数据源不提供换手率时可显式允许缺失，并降低评分完整性。
